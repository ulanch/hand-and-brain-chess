# Port the Express/WebSocket server listens on.
# Defaults to 3000 when unset.
PORT=3000

# Allowed CORS origin (the client's URL).
# Defaults to http://localhost:5173 (Vite dev server) when unset.
CLIENT_ORIGIN=http://localhost:5173
