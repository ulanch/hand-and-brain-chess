# Base URL of the Express server (HTTP). The WebSocket URL is derived from this
# automatically (http → ws, https → wss).
# Defaults to http://localhost:3000 when unset.
VITE_SERVER_URL=http://localhost:3000
